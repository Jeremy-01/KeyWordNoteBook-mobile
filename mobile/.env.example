# 移动端 API 地址示例
# 当前应用通过 --dart-define=API_BASE_URL=... 读取配置，不会自动加载此文件。
# Android 模拟器: flutter run --dart-define=API_BASE_URL=http://10.0.2.2:8000/api/v1
# Android 真机（USB）: adb reverse tcp:8000 tcp:8000 && flutter run
API_BASE_URL=http://localhost:8000/api/v1

# 调试模式
DEBUG=true
